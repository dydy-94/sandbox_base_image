# aio-sandbox Python server requirements. Pinned to versions that
# survive the offline build because of the conflict between the
# standalone aio-server code (which imports `from openhands.*`, etc.)
# and modern FastAPI / starlette (which removed `add_event_handler` in
# 0.110+). The api-requires-`add_event_handler` constraint mandates
# `fastapi<0.110` AND `pydantic<3`. The `mcp` lib requires
# `starlette>=0.48`, which conflicts. We resolve that by pinning to
# `fastapi<0.110` and accepting `starlette=0.36.x`.
#
# 47 runtime deps + 3 fixup deps (fastmcp<2, starlette<0.37,
# jupyterlab>=4).  See docker/OFFLINE-BUILD-FIX.md §4.6 / §4.9.

# ---------- Web framework & async -----------------------------------------
uvicorn[standard]>=0.30.0,<0.32.0
fastapi>=0.95.0,<0.110
aiohttp>=3.9.0
websockets>=12.0
uvloop>=0.19.0
starlette>=0.36.0,<0.37

# ---------- Data validation / settings ------------------------------------
pydantic>=2.6.0,<3
pydantic-settings>=2.2.0
python-multipart>=0.0.9

# ---------- Browser automation --------------------------------------------
playwright>=1.42.0

# ---------- Templating / CLI / HTTP clients -------------------------------
Jinja2>=3.1.0
click>=8.1.0
httpx>=0.27.0
httpx-sse>=0.4.0
email-validator>=2.1.0
requests>=2.31.0

# ---------- Logging / fs / tasks ------------------------------------------
loguru>=0.7.0
aiofiles>=23.2.0
anyio>=4.3.0
python-json-logger>=2.0.7

# ---------- Auth / cryptography -------------------------------------------
pyjwt>=2.8.0
cachelib>=0.13.0
itsdangerous>=2.1.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
cryptography>=42.0.0,<45

# ---------- Terminal / shell ---------------------------------------------
bashlex>=0.18
pyperclip>=1.8.2
termcolor>=2.4.0
libtmux>=0.15.0

# ---------- Local cache / fs / async -------------------------------------
aiosqlite>=0.20.0,<0.21
watchdog>=4.0.0

# ---------- Jupyter (required by python-server JupyterService) ----------
# v13-fix9.11: jupyter removed.  base image bakes DISABLE_JUPYTER=true
# (Dockerfile.offline L921) and python-server skips loading
# app.services.jupyter / app.api.v1.jupyter when that env is true.  We
# also drop jupyter-client here so the python-server venv does not pull
# in ipykernel / jupyter_client / nbformat transitively — this removes
# ~30 MB of deps and eliminates the L152-174 NoSuchKernel startup ERROR.
# To re-enable jupyter: set DISABLE_JUPYTER=false (or unset it) AND
# uncomment the jupyter-client line below AND install an ipykernel
# kernel spec (e.g. `python -m ipykernel install --name python3`).
# jupyter-client>=8.6.3

# ---------- MCP ----------------------------------------------------------
mcp>=1.0.0
fastmcp>=1.0.0,<2.0

# ---------- Misc ---------------------------------------------------------
terminado>=0.18.0
pexpect>=4.9.0
redis>=5.0.0
msgpack>=1.0.7
mmh3>=4.1.0
cloudpickle>=3.0.0
pathspec>=0.12.0
gitpython>=3.1.0
fuzzywuzzy>=0.18.0
python-levenshtein>=0.25.0
chardet>=5.2.0
tabulate>=0.9.0
